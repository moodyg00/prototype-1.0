# Worker HTTP server — see docs/DEPLOYMENT.md
WORKER_PORT=3003
ADMIN_BASE_URL=http://localhost:3001

# Must match admin Settings → Business (Cron secret)
# Or bootstrap via CRON_SECRET in apps/admin/.env.local + integrations:bootstrap
CRON_SECRET=
