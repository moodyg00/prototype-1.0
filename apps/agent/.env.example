# Database — local dev (see apps/agent/.env.local; never commit real values)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prototype"
DIRECT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prototype"

# Production Supabase Postgres (host only — no Supabase Auth):
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# DIRECT_DATABASE_URL=postgresql://postgres.[project-ref]:[password]@[region].pooler.supabase.com:5432/postgres

# Auth — match admin in production (shared proto_session cookie)
AUTH_REQUIRED=false
AUTH_SECRET=
AUTH_COOKIE_DOMAIN=

NEXT_PUBLIC_APP_URL=http://localhost:3002

# Integrations (bootstrap → DB via admin, or set here for first import)
MERCURY_API_KEY=

# Agent runtime
XAI_API_KEY=

# Workflow run traces are recorded natively in the WorkflowRun table — no external
# tracing service required. View them in the Runs panel.
# CSUITE_MODEL=grok-4.3
