# Prevent execution of scripts in uploads directory
<FilesMatch "\.(php|exe|pl|cgi|scr|asp)$">
Deny from all
</FilesMatch>

# Allow access to image files
<FilesMatch "\.(jpg|jpeg|png|gif)$">
Allow from all
</FilesMatch>